name: "[DEPRECATED] Deploy Legacy Edge Gateway"

# =============================================================================
# DEPRECATED
# =============================================================================
# This workflow is kept for historical reference only.
# The project is migrating to full containerized deployment.
# =============================================================================

on:
  workflow_dispatch:


jobs:
  deprecated:
    runs-on: ubuntu-latest
    steps:
      - name: Deprecation notice
        run: |
          echo "This workflow is deprecated and intentionally disabled."
          echo "Use containerized deployment instead."
          exit 1
