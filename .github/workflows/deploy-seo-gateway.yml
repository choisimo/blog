name: "[DEPRECATED] Deploy SEO Gateway"

on:
  workflow_dispatch:

jobs:
  deprecated:
    runs-on: ubuntu-latest
    name: Deprecated
    steps:
      - name: Deprecation notice
        run: |
          echo "This workflow is deprecated and intentionally disabled."
          echo "Use containerized deployment instead."
          exit 1
