# Backend Capability Map

| Domain | Public Entry | Worker Handler | Backend Handler | Auth Boundary | Primary Data Store | Realtime Mode | Authoritative Runtime | Notes |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| auth | `/api/v1/auth` | `workers/api-gateway/src/routes/auth.ts` | `backend/src/routes/auth.js` | Worker auth details are not confirmed in the required files. Backend mount is behind `requireBackendKey` in `backend/src/index.js`. | Not confirmed in required files | None confirmed in required files | Not confirmed in required files | Both runtimes register this prefix in the required files, but the ownership split is not confirmed here. |
| chat | `/api/v1/chat` | `workers/api-gateway/src/routes/chat.ts` | `backend/src/routes/chat.js` | Worker auth details are not confirmed in the required files. Backend mount is behind `requireBackendKey` in `backend/src/index.js`. | Not confirmed in required files | WebSocket initialized by `initChatWebSocket(server)` in `backend/src/index.js` | Backend | `backend/README.md` names chat as an origin-dependent backend function, and backend startup explicitly initializes chat websocket support. |
| search | `/api/v1/search` | `workers/api-gateway/src/routes/search.ts` | `backend/src/routes/search.js` | Worker auth details are not confirmed in the required files. Backend mount is behind `requireBackendKey` in `backend/src/index.js`. | Not confirmed in required files | None confirmed in required files | Not confirmed in required files | Both runtimes register this prefix in the required files, but the ownership split is not confirmed here. |
| rag | `/api/v1/rag` | `workers/api-gateway/src/routes/rag.ts` | `backend/src/routes/rag.js` | Worker auth details are not confirmed in the required files. Backend mount is behind `requireBackendKey` in `backend/src/index.js`. | Not confirmed in required files | None confirmed in required files | Backend | `backend/README.md` names RAG as an origin-dependent backend function. |
| agent | `/api/v1/agent` | `workers/api-gateway/src/index.ts` fallback proxy | `backend/src/routes/agent.js` | Requests reach the backend through worker fallback. Backend mount is behind `requireBackendKey` in `backend/src/index.js`. | Not confirmed in required files | None confirmed in required files | Backend | The required worker files do not mount a dedicated `/agent` route. Access goes through the worker fallback to the backend handler. |
| execute | `/api/v1/execute` | `workers/api-gateway/src/index.ts` fallback proxy | `backend/src/routes/execute.js` | Requests reach the backend through worker fallback. Backend mount is behind `requireBackendKey` in `backend/src/index.js`. | Not confirmed in required files | None confirmed in required files | Backend | The required worker files do not mount a dedicated `/execute` route. Access goes through the worker fallback to the backend handler. |
| notifications | `/api/v1/notifications` | `workers/api-gateway/src/routes/notifications.ts` | `backend/src/routes/notifications.js` | Worker `/stream` uses `requireAuth`. Backend `/stream` uses `requireUserAuth`. Backend `/push` and `/health` use `requireBackendKey`. | Not confirmed in required files | SSE | Backend | Backend owns the notifications stream and control endpoints. The worker path is a proxy to backend SSE and should stay proxy-only. |
| admin | `/api/v1/admin` | `workers/api-gateway/src/index.ts` fallback proxy | `backend/src/routes/admin.js` | Requests reach the backend through worker fallback. Backend mount is behind `requireBackendKey` in `backend/src/index.js`. | Not confirmed in required files | None confirmed in required files | Backend | The required worker files mount `/admin/ai`, `/admin/secrets`, and `/admin/logs`, but not a generic `/admin` route. Generic `/api/v1/admin` traffic therefore falls through to the backend in this file set. |
| comments | `/api/v1/comments` | `workers/api-gateway/src/routes/comments.ts` | None | Worker serves public comment reads, writes, reactions, SSE, and admin delete directly from D1. Backend legacy comment routes were removed so the worker path remains the only authoritative surface. | Worker D1 comment store and reaction state | SSE | Worker | Comments are edge-native again. Future changes should keep worker D1 as the source of truth unless ownership is explicitly migrated with accompanying docs and snapshot updates. |

## Change Rules

- Future route changes must update this file in the same PR.
- Keep this file as the single ownership inventory for backend capability mapping.
- Do not mark `comments` or `notifications` as shared or mixed authority in future edits.
