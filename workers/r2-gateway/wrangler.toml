name = "r2-gateway"
main = "src/index.ts"
compatibility_date = "2025-10-21"

# R2 bucket binding (replace with your actual bucket name)
[[r2_buckets]]
binding = "MY_BUCKET"
bucket_name = "blog"

# workers.dev subdomain is automatically enabled
# No routes needed for Service Binding - it works directly

# Option: attach custom domains if needed for external access
# [triggers]
# custom_domains = ["r2-assets.nodove.com"]
