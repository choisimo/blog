# =============================================================================
# Local Development Environment Variables
# =============================================================================
#
# ⚡ QUICK START (No configuration needed!)
# ----------------------------------------
# Just run: ./start-all-local.sh
# 
# The script automatically creates .env.local with secure random keys.
# All non-AI services work immediately (posts, comments, terminal, etc.)
#
# 🤖 TO ENABLE AI FEATURES (Choose one option)
# --------------------------------------------
#
# OPTION 1: Cloud AI (recommended for quality)
#   Add one API key below:
#   - GOOGLE_API_KEY=AIza...   (free: https://aistudio.google.com/app/apikey)
#   - OPENAI_API_KEY=sk-...    (paid: https://platform.openai.com/api-keys)
#   - ANTHROPIC_API_KEY=sk-... (paid: https://console.anthropic.com/)
#
# OPTION 2: Local AI with Ollama (free, offline)
#   1. Install Ollama: https://ollama.ai
#   2. Run: ollama serve
#   3. Pull model: ollama pull llama3.2
#   4. Use model name: "local" or "local/llama3"
#
# =============================================================================

# -----------------------------------------------------------------------------
# LiteLLM Gateway Configuration
# -----------------------------------------------------------------------------
# Master key for LiteLLM API authentication (auto-generated if not set)
LITELLM_MASTER_KEY=sk-local-dev-key

# Default AI model
# Cloud options: gemini-1.5-flash, gpt-4o-mini, claude-3-haiku
# Local option: local (requires Ollama running)
AI_DEFAULT_MODEL=gemini-1.5-flash

# -----------------------------------------------------------------------------
# AI Provider API Keys (at least one required)
# -----------------------------------------------------------------------------
# Google Gemini (recommended - free tier available)
# Get it from: https://aistudio.google.com/app/apikey
GOOGLE_API_KEY=

# OpenAI (optional)
# Get it from: https://platform.openai.com/api-keys
OPENAI_API_KEY=

# Anthropic Claude (optional)
# Get it from: https://console.anthropic.com/
ANTHROPIC_API_KEY=

# -----------------------------------------------------------------------------
# Terminal Service
# -----------------------------------------------------------------------------
# Secret key for terminal WebSocket authentication
ORIGIN_SECRET_KEY=local-dev-secret-change-me

# Docker image for terminal sandbox
SANDBOX_IMAGE=alpine:latest

# -----------------------------------------------------------------------------
# Admin Configuration (Optional)
# -----------------------------------------------------------------------------
# Bearer token for admin API routes
ADMIN_BEARER_TOKEN=local-admin-token

# JWT secret for admin login
JWT_SECRET=local-jwt-secret-change-me

# Admin credentials
ADMIN_USERNAME=admin
ADMIN_PASSWORD=admin123

# -----------------------------------------------------------------------------
# GitHub Integration (Optional - for PR creation)
# -----------------------------------------------------------------------------
# GITHUB_TOKEN=
# GITHUB_REPO_OWNER=your-username
# GITHUB_REPO_NAME=blog

# -----------------------------------------------------------------------------
# Cloudflare Configuration (Not needed - workers are emulated locally)
# -----------------------------------------------------------------------------
# Only needed if connecting to remote Cloudflare services
# CF_ACCOUNT_ID=
# CF_API_TOKEN=
# D1_DATABASE_ID=
