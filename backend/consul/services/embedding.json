{
  "service": {
    "id": "embedding-server",
    "name": "embedding",
    "tags": ["ai", "tei", "embedding"],
    "port": 80,
    "check": {
      "http": "http://embedding-server:80/health",
      "interval": "30s",
      "timeout": "10s"
    },
    "meta": {
      "type": "embedding",
      "model": "all-MiniLM-L6-v2"
    }
  }
}
